void backtrack(result, path, k, input) 
{ 
	if (isSolution) { 
		k==input.length, target==0
		result.add(path); return;
		return, return true, return 1
	} 
	candidates = construct; 
	for (c : candidates) { 
		backtrack(result, path, k+1, input); 
	} 
}
